import { AppLayout } from "@/components/AppLayout";

const Settings = () => {
  return (
    <AppLayout>
      <h1 className="text-2xl font-extrabold text-foreground mb-2">Configurações</h1>
      <p className="text-sm text-muted-foreground">Em breve: edite seu perfil, número de WhatsApp e preferências de notificação.</p>
    </AppLayout>
  );
};

export default Settings;
